/* 지갑 화면이 켜질 때 하는 세 가지.
 *
 * 🔴 **왜 파일로 뺐나.** 이 셋은 원래 `wallet.html` 안의 인라인 스크립트였다.
 *    그런데 이 화면의 CSP 는 `script-src 'self'` 다 — 12단어가 든 화면이라
 *    인라인을 막는 것이 맞다. 그래서 **셋 다 실행되지 않고 있었다.**
 *    오류는 콘솔에만 남아서 화면은 멀쩡해 보였다. 실제로 죽은 것은 둘이다:
 *      · 「?」 설명이 안 떴다 — `help.js` 가 읽는 `PLAYX_HELP` 가 안 세워졌다
 *      · 안드로이드에서 「앱으로 설치」가 안 떴다 — 서비스워커가 등록되지 않았다
 *    번역은 안 죽었다. `i18n.js` 가 `autoTranslate()` 로 **스스로** 부르고
 *    `MutationObserver` 로 다시 부른다. 아래 한 줄은 그래서 중복이지만,
 *    첫 그림을 한 박자 빨리 바꿔 주므로 그대로 둔다.
 *    (실측 2026-09-06. 지갑이 안 열린다는 신고를 쫓다가 같이 나왔다.)
 *
 * 🔴 CSP 해시로 때우지 않는다. 글자 하나만 바뀌어도 또 조용히 죽는다.
 *    파일로 두면 `'self'` 로 통과하고, 바뀌어도 계속 돈다.
 *
 * ⚠️ 이 파일은 **본문 맨 끝**에서 불러야 한다. `translateDom(document.body)` 가
 *    아래 탭 막대까지 번역해야 하는데, 위에서 부르면 그때는 탭이 아직 없다.
 */

/* 「?」를 눌러 보는 설명. 좁은 선택자가 이긴다(`help.js` 참고). */
window.PLAYX_HELP = {
  '#btn-new': ['이 폰에 새 지갑을 만듭니다.',
    '만들면 12단어가 나옵니다. 그 열두 낱말이 이 지갑의 전부입니다 — 종이에 적어 두세요.'],
  '#btn-restore': ['전에 쓰던 지갑을 12단어로 되살립니다.',
    '같은 12단어면 어느 기계에서든 같은 지갑이 됩니다.'],
  '#me-name': ['방과 쪽지에서 남에게 보이는 이름입니다.',
    '비워 두셔도 됩니다. 그러면 열쇠 앞 8글자로 보이고, 그게 익명입니다.'],
  '#me-save': ['적은 이름표를 세상에 올립니다.',
    '이름은 누구나 같게 달 수 있습니다. 그래서 이름 옆 색은 이름이 아니라 열쇠에서 나옵니다.'],
  '#mebox': ['남에게 보이는 내 이름·얼굴을 정하는 곳입니다.', ''],
  '#fanbox': ['내 자산을 가진 사람들의 방에 한 번에 알립니다.',
    '자산이 있는 분만 쓰는 기능입니다.'],
  'a[href^="/shops"]': ['가게와 중고 물건을 보는 곳입니다.', ''],
};

// 손님 폰이 정한 말로 화면을 바꾼다. 없는 문장은 한국어
// 그대로 둔다 — 빈 자리보다 낫다.
window.translateDom && translateDom(document.body);

// 🔴 안드로이드가 「앱으로 설치」를 권하려면 **서비스워커가 이 화면에
//    걸려 있어야** 한다. 없어서 설치 단추가 영영 안 그려졌다.
//    `sw.js` 는 일부러 아무것도 캐시하지 않는다 — 지갑 코드를 캐시하면
//    보안 수정이 안 먹는다. 설치를 가능하게 하는 것이 이것의 일이다.
if ('serviceWorker' in navigator) {
  window.addEventListener('load', function () {
    navigator.serviceWorker.register('/sw.js').catch(function () {});
  });
}
