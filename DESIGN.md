# PLAY X Raven — 설계 결정 기록

2026-08-18. 결정한 이유까지 적는다. 나중에 "왜 이렇게 했지" 하고 되돌리는 것을 막기 위해서다.

---

## 이 프로그램이 하는 일

켜놓은 컴퓨터 한 대를 **내 노드 + 내 창고 + 내 상점**으로 만든다.
설치는 하나. 노드와 IPFS를 따로 깔지 않는다.

```
[ PLAY X Raven ]
   ├─ ravend        노드          (번들, 기존 데이터 재사용)
   ├─ kubo          IPFS         (번들, 기존 repo 재사용)
   ├─ 자산 보존      자동 핀
   ├─ 발행          AI 도우미 + IPFS 업로드
   ├─ 가게          아토믹 스왑 주문서 + 메뉴판
   └─ 로컬 웹서버    주인 폰에서 조작
```

---

## 화면: 사이드바. 한 페이지에 쌓지 않는다

| 위치 | 하는 일 | 돈이 나가나 |
|---|---|---|
| 자산 | 보존 (핀) | **아니오** |
| 지갑 | 잔액·받기·보내기·내역 | 예 |
| 발행 | 되돌릴 수 없는 마법사 | 예 (소각) |
| 가게 | 주문서 + 메뉴판 | 예 |
| 설정 | 노드·수수료·외부접속 | 아니오 |

**이유:** 자산 화면이 안전한 것은 보내는 버튼이 없기 때문이다. 여기에 잔액과
주문을 얹으면 "클릭해도 되는 화면"과 "영구히 돈을 태우는 화면"이 같은 손 버릇으로
묶인다. 가게는 하나다 — 벤딩과 POS는 같은 일이다(고르고, 주소가 생기고, 받고, 보낸다).
손님 화면은 사이드바에 없다. 손님은 이 프로그램을 열지 않는다.

---

## 자산 보존 (완성됨)

- 목록이 뜨면 **자동으로** 생존 확인. 묻지 않는다. 이 앱을 연 이유가 그 확인이다.
- 이미 보존 중인 것은 확인하지 않는다. 이 컴퓨터에 있다. 끝난 일이다.
- **순차 처리.** 병렬로 던지면 DHT 질의가 포화돼 산 자산이 죽은 것으로 보인다.
  느린 진실이 빠른 오진보다 안전하다.
- 진행 표시는 `7/24 · 중지`. **가짜 퍼센트 바를 쓰지 않는다** — 어떤 해시는 0.4초,
  어떤 것은 20초다. 추측 진행률은 느린 것보다 거짓말로 읽힌다.
- **중지 버튼은 필수.** 멈출 수 없는 백그라운드 작업은 지갑 옆에서 신뢰가 아니다.
- 상태는 `사망`이 아니라 **`찾지 못함`**. 20초간 1바이트를 못 읽었다는 뜻이지
  사망 진단이 아니다. 빨간색은 파괴 동작(보존 해제)에만 쓴다.
- 미리보기는 **모달이 아니라 옆 패널**. 목록이 살아 있어야 한다.
  행 클릭 = 보기, 보존 버튼 = 보관. **한 클릭이 두 의미를 가지면 사용자가 망설인다.**
- PDF는 `sandbox` iframe으로만. 스크립트·폼·팝업 차단. 누가 올렸는지 모르는 파일이다.
- **`127.0.0.1:8080`만 사용.** 공개 게이트웨이로 넘어가는 순간 이 지갑이 무엇을
  들고 있는지 바깥에 찍힌다.

---

## 가게: 아토믹 스왑 (RIP-15)

입금 감지 후 자동 발송이 **아니다.**

```
판매자가 부분 서명 주문서를 만든다 (SIGHASH_SINGLE | SIGHASH_ANYONECANPAY)
        ↓
아무 데나 공개한다 (QR, 웹, 메신저)
        ↓
구매자가 자기 입력·출력을 붙여 완성한다
        ↓
한 트랜잭션 안에서 양쪽이 동시에 받는다
```

**왜 이쪽인가:**

| | 입금 감지 후 발송 | 아토믹 스왑 |
|---|---|---|
| 지갑 잠금 해제 | 상시 필요 | **불필요** |
| 컴퓨터 켜두기 | 필수 | **불필요** |
| 발송 로직 버그 | 곧 자산 손실 | 없음 |
| 사기 | 가능 | **불가능** (둘 다 받거나 아무것도 안 됨) |

RVN 받을 주소를 **콜드 지갑으로 지정**하면 대금이 핫 지갑을 아예 안 거친다.

참고 구현: `ben-abraham/raven-trader-pro` (Python), `raven-trader` (C#)

---

## 자동 발송이 필요한 경우 (현장 결제 등)

**앱의 전송 정책은 ravend를 구속할 수 없다.** `.cookie` + 잠금 해제된 키는
이 컴퓨터의 어떤 프로세스든 인출할 수 있다는 뜻이다. 타이머·자동잠금·정책은
실수 방지 장치이지 보안 경계가 아니다.

유일한 실질적 통제는 **지갑 분리**다.

```
가게 지갑   오늘 팔 재고만        ← 털려도 손실 한정
콜드 지갑   본 자산               ← 인터넷에 없음
```

정산은 **알림 + 승인**:
> "1,240 RVN 모였습니다. 옮길까요?" → 주인이 폰에서 확인

완전 자동은 키를 상시 열어둬야 하고, 완전 수동은 결국 안 하게 된다.
하루 한 번 터치가 실제로 지켜지는 유일한 선이다.

**절대 하지 않는 것:** 무기한 잠금 해제, 암호 저장.

---

## 발행

되돌릴 수 없고 RVN이 소각된다.

- 모달과 "이해합니다" 체크박스는 그냥 클릭된다. **이름을 직접 타이핑**해서 확인하게 한다.
- `reissuable = true`가 유일하게 정직한 기본값. 되돌릴 수 없는 쪽을 기본으로 두면 안 된다.
- AI는 **제안만** 한다. 이름 규칙 검증(`^[A-Z0-9._]{3,}$`), 중복 조회, 소각 비용
  계산은 코드가 한다. AI는 `PLAYX/음악앨범` 같은 불가능한 이름을 태연히 내놓는다.

---

## 수수료: 판매액의 1%

- **끄는 스위치를 두지 않는다.** 트랜잭션을 열어보면 드러나는 것을 설정으로 숨기면
  그게 더 나쁘다. 발각되는 순간 잃는 것은 수수료가 아니라 신뢰 전부다.
- 판매자 정산에만 적용. 손님이 내는 금액에는 영향 없음. 99%가 판매자에게.
- 받는 주소를 설정 화면에 **전체 노출**.
- 채굴 수수료와는 별개임을 명시.
- 근거: 카드 오프라인 2.0~2.3%, 온라인 PG 2.7~3.5%, 크립토 결제 업계 표준 1%.

**발행에는 수수료를 받지 않는다.** 발행은 이미 RVN 소각으로 비용을 낸다.
거기 얹으면 "왜 중간에서 뜯냐"가 된다.

---

## 손님은 우리 컴퓨터에 접속하지 않는다

```
메뉴·상품 사진   → IPFS (게이트웨이 링크. 집 컴퓨터는 NAT 뒤라 직접 접속이 어렵다)
주문             → 체인 입금 또는 아토믹 스왑 주문서
발송             → 스왑이 완결하거나, 앱이 감지 후 처리
```

포트포워딩도, 고정 IP도, 중계 서버도 필요 없다.

**주인 접속은 다른 문제다.** 전국 어디서든 관리하려면 메시 VPN(Tailscale 등)이
맞다 — 공개되지 않고, 공유기를 건드리지 않는다. 공개 터널을 열면 위의 지갑 분리가
선택이 아니라 필수가 된다.

---

## 저사양 PC 지원

레이븐은 프루닝을 지원한다 (`-prune=<n>`, `src/init.cpp:472`).

| | 기본 | 저사양 모드 |
|---|---|---|
| 체인 | 45 GB | ~5 GB |
| 메모리 | 1.2 GB (실측) | ~600 MB (`dbcache` 축소) |
| IPFS | 10 GB | 2 GB |

8GB 램 사무실 PC에서 돌아가야 한다. 설정에서 스위치 하나로.

---

## 만들지 않는 것

- **또 하나의 지갑.** 코어·Electrum·MoonTree·웹월렛이 이미 있다. 필요한 만큼만 둔다.
- 우리가 운영하는 IPFS 호스팅. 사용자가 늘수록 비용이 는다. 각자 자기 컴퓨터에서.
- 손님용 앱. 손님은 브라우저면 된다.
- 숨긴 수수료.

---

## 생태계에서 가져올 것

| 프로젝트 | 쓸 곳 |
|---|---|
| RIP-15 / `raven-trader-pro` | 아토믹 스왑 구현 참고 |
| Squawker Protocol | 자산+IPFS 메시징 — 손님 대화 |
| `ravenrebels/*` | 웹 쪽 JS 도구 |
| Electrum Ravencoin | 자산·IPFS 처리 참고 |

**아무도 안 하고 있는 것:** 자산 IPFS 자동 보존, 노드+IPFS 한 프로그램, 가게 운영.
지갑은 이미 많다.
